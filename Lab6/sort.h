#ifndef SORT_H
#define SORT_H

class Sort{ // class Sort which holds all the sorting functions
public:

  //prototypes of sorting functions
  void swap(int*, int*);
  void bubbleSort(int*, int);
  void selectionSort(int*, int);
  void insertionSort(int*, int);
  void printArray(int*, int);
};

#endif
